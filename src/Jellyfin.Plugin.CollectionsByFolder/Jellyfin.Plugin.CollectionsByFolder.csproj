<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>

    <!-- nur die Assembly erzeugen, keine Core-DLLs mitpacken -->
    <CopyLocalLockFileAssemblies>false</CopyLocalLockFileAssemblies>

    <!-- Eine Version reicht; keine zusätzlichen AssemblyInfo-Attribute -->
    <Version>0.1.3.0</Version>
  </PropertyGroup>

  <ItemGroup>
    <!-- Jellyfin APIs nur zum Kompilieren, nicht mit ausliefern -->
    <PackageReference Include="Jellyfin.Common" Version="10.10.0">
      <ExcludeAssets>runtime;native;contentfiles;analyzers;buildtransitive</ExcludeAssets>
    </PackageReference>
    <PackageReference Include="Jellyfin.Model" Version="10.10.0">
      <ExcludeAssets>runtime;native;contentfiles;analyzers;buildtransitive</ExcludeAssets>
    </PackageReference>
    <PackageReference Include="Jellyfin.Controller" Version="10.10.0">
      <ExcludeAssets>runtime;native;contentfiles;analyzers;buildtransitive</ExcludeAssets>
    </PackageReference>
    <PackageReference Include="Microsoft.AspNetCore.Mvc.Core" Version="2.2.5">
      <ExcludeAssets>runtime;native;contentfiles;analyzers;buildtransitive</ExcludeAssets>
    </PackageReference>
  </ItemGroup>

  <!-- Eingebettete Web-Ressourcen -->
  <ItemGroup>
    <EmbeddedResource Include="Configuration/index.html" />
    <EmbeddedResource Include="Configuration/index.js" />
  </ItemGroup>

</Project>
